\_\_ Optimization \_\_ splitChunks

# \_ Setup

1. В объекте `splitChunks` создать свойство `chunks` и присвоить ему значение `"all"`:

   ```
   optimization: {
    splitChunks: {
      chunks: "all"
    }
   }
   ```
